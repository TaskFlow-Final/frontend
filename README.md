# TaskFlow Frontend

React 19 + Vite frontend for the TaskFlow API.

```bash
npm install
npm run dev
```

The frontend uses `https://taskflow-backend-ivsy.onrender.com` by default.
Set `VITE_API_URL` in `.env` to use a different backend URL.

```bash
npm run build
```
